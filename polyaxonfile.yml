---
version: 1

kind: experiment

framework: tensorflow

# environment:
#   node_selector:
#     polyaxon: general


logging:
  level: DEBUG

build:
  backend: native  # kaniko
  image: tensorflow/tensorflow:2.2.0rc4
  build_steps:
    - pip install polyaxon-client
    - pip install boto3
params:
  num_epochs: 30

run:
  cmd:  python model_tf1.4.py --num_epochs={{ num_epochs }}