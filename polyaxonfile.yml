---
version: 1

kind: experiment

framework: tensorflow

# environment:
#   node_selector:
#     polyaxon: general

build:
  backend: native  # kaniko
  image: tensorflow/tensorflow:1.4.1-py3
  build_steps:
    - pip install polyaxon-client
    - pip install boto3
    - pip install botocore

run:
  cmd:  python3 model.py  # with default params

logging:
  level: DEBUG  # DEBUG level will also show S3 err in case of network issues
